<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ContractorHub — The Smart Way to Hire</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Outfit:wght@300;400;500&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --cream: #F5F0E8;
      --ink: #1A1714;
      --warm: #C4873A;
      --warm-light: #F0DFC0;
      --muted: #8A8178;
      --border: #E0D8CC;
      --white: #FDFAF5;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'Outfit', sans-serif;
      background: var(--cream);
      color: var(--ink);
      overflow-x: hidden;
    }

    /* NAV */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1.25rem 3rem;
      background: rgba(245, 240, 232, 0.85);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
    }
    .nav-logo {
      font-family: 'Playfair Display', serif;
      font-size: 22px;
      color: var(--ink);
      text-decoration: none;
      letter-spacing: -0.01em;
    }
    .nav-links {
      display: flex;
      align-items: center;
      gap: 2rem;
    }
    .nav-links a {
      font-size: 14px;
      font-weight: 400;
      color: var(--muted);
      text-decoration: none;
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--ink); }
    .nav-cta {
      padding: 9px 22px;
      background: var(--ink);
      color: var(--cream) !important;
      border-radius: 100px;
      font-size: 13px !important;
      font-weight: 500 !important;
      transition: opacity 0.2s !important;
    }
    .nav-cta:hover { opacity: 0.8; }

    /* HERO */
    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 8rem 2rem 4rem;
      position: relative;
      overflow: hidden;
    }
    .hero::before {
      content: '';
      position: absolute;
      top: -20%;
      left: 50%;
      transform: translateX(-50%);
      width: 800px;
      height: 800px;
      background: radial-gradient(ellipse, rgba(196,135,58,0.12) 0%, transparent 70%);
      pointer-events: none;
    }
    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 6px 16px;
      background: var(--warm-light);
      border: 1px solid rgba(196,135,58,0.3);
      border-radius: 100px;
      font-size: 12px;
      font-weight: 500;
      color: var(--warm);
      letter-spacing: 0.04em;
      text-transform: uppercase;
      margin-bottom: 2rem;
      opacity: 0;
      animation: fadeUp 0.6s ease 0.1s forwards;
    }
    .hero-badge::before { content: '●'; font-size: 8px; }

    .hero h1 {
      font-family: 'Playfair Display', serif;
      font-size: clamp(42px, 7vw, 80px);
      font-weight: 700;
      line-height: 1.08;
      letter-spacing: -0.02em;
      max-width: 820px;
      margin-bottom: 1.5rem;
      opacity: 0;
      animation: fadeUp 0.7s ease 0.2s forwards;
    }
    .hero h1 em {
      font-style: italic;
      color: var(--warm);
    }
    .hero-sub {
      font-size: clamp(15px, 2vw, 18px);
      font-weight: 300;
      color: var(--muted);
      max-width: 480px;
      line-height: 1.7;
      margin-bottom: 2.5rem;
      opacity: 0;
      animation: fadeUp 0.7s ease 0.35s forwards;
    }
    .hero-actions {
      display: flex;
      align-items: center;
      gap: 1rem;
      flex-wrap: wrap;
      justify-content: center;
      opacity: 0;
      animation: fadeUp 0.7s ease 0.5s forwards;
    }
    .btn-main {
      padding: 14px 32px;
      background: var(--ink);
      color: var(--cream);
      border-radius: 100px;
      font-size: 15px;
      font-weight: 500;
      text-decoration: none;
      transition: transform 0.2s, opacity 0.2s;
      font-family: 'Outfit', sans-serif;
    }
    .btn-main:hover { opacity: 0.85; transform: translateY(-1px); }
    .btn-ghost {
      padding: 14px 32px;
      border: 1px solid var(--border);
      color: var(--ink);
      border-radius: 100px;
      font-size: 15px;
      font-weight: 400;
      text-decoration: none;
      transition: background 0.2s;
      font-family: 'Outfit', sans-serif;
    }
    .btn-ghost:hover { background: var(--warm-light); border-color: var(--warm); }

    .hero-stats {
      display: flex;
      gap: 3rem;
      margin-top: 4rem;
      padding-top: 3rem;
      border-top: 1px solid var(--border);
      opacity: 0;
      animation: fadeUp 0.7s ease 0.65s forwards;
    }
    .hero-stat-num {
      font-family: 'Playfair Display', serif;
      font-size: 32px;
      color: var(--ink);
    }
    .hero-stat-label {
      font-size: 12px;
      color: var(--muted);
      letter-spacing: 0.06em;
      text-transform: uppercase;
      margin-top: 3px;
    }

    /* MARQUEE */
    .marquee-wrap {
      overflow: hidden;
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      padding: 14px 0;
      background: var(--white);
    }
    .marquee-track {
      display: flex;
      gap: 2rem;
      animation: scroll 28s linear infinite;
      width: max-content;
    }
    .marquee-item {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 13px;
      font-weight: 400;
      color: var(--muted);
      white-space: nowrap;
      padding: 0 1rem;
    }
    .marquee-item span { color: var(--warm); font-size: 10px; }

    /* HOW IT WORKS */
    .section {
      padding: 7rem 3rem;
      max-width: 1100px;
      margin: 0 auto;
    }
    .section-eyebrow {
      font-size: 11px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--warm);
      margin-bottom: 1rem;
      font-weight: 500;
    }
    .section-title {
      font-family: 'Playfair Display', serif;
      font-size: clamp(28px, 4vw, 44px);
      font-weight: 700;
      letter-spacing: -0.02em;
      line-height: 1.15;
      max-width: 560px;
      margin-bottom: 4rem;
    }
    .steps {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
    }
    .step {
      padding: 2rem;
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 16px;
      position: relative;
      transition: transform 0.2s, border-color 0.2s;
    }
    .step:hover { transform: translateY(-4px); border-color: var(--warm); }
    .step-num {
      font-family: 'Playfair Display', serif;
      font-size: 48px;
      color: var(--warm-light);
      line-height: 1;
      margin-bottom: 1.25rem;
      font-weight: 700;
    }
    .step h3 {
      font-size: 17px;
      font-weight: 500;
      margin-bottom: 0.75rem;
      letter-spacing: -0.01em;
    }
    .step p { font-size: 14px; color: var(--muted); line-height: 1.7; font-weight: 300; }

    /* CATEGORIES */
    .cats-section {
      background: var(--white);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      padding: 7rem 3rem;
    }
    .cats-inner { max-width: 1100px; margin: 0 auto; }
    .cats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
      gap: 12px;
      margin-top: 3rem;
    }
    .cat-card {
      padding: 1.5rem 1.25rem;
      border: 1px solid var(--border);
      border-radius: 12px;
      cursor: pointer;
      transition: all 0.2s;
      text-decoration: none;
      display: block;
    }
    .cat-card:hover { background: var(--warm-light); border-color: var(--warm); transform: translateY(-2px); }
    .cat-icon { font-size: 28px; margin-bottom: 10px; }
    .cat-name { font-size: 14px; font-weight: 500; color: var(--ink); }
    .cat-count { font-size: 12px; color: var(--muted); margin-top: 3px; }

    /* TESTIMONIALS */
    .testi-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.25rem;
      margin-top: 3rem;
    }
    .testi-card {
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 1.75rem;
      transition: transform 0.2s;
    }
    .testi-card:hover { transform: translateY(-3px); }
    .testi-stars { color: var(--warm); font-size: 14px; margin-bottom: 1rem; }
    .testi-text { font-size: 14px; color: var(--muted); line-height: 1.75; font-weight: 300; font-style: italic; margin-bottom: 1.25rem; }
    .testi-author { display: flex; align-items: center; gap: 10px; }
    .testi-av {
      width: 36px; height: 36px; border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      font-size: 12px; font-weight: 500;
      background: var(--warm-light); color: var(--warm);
    }
    .testi-name { font-size: 13px; font-weight: 500; }
    .testi-role { font-size: 11px; color: var(--muted); }

    /* CTA BANNER */
    .cta-banner {
      margin: 0 3rem 7rem;
      background: var(--ink);
      border-radius: 24px;
      padding: 5rem 4rem;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    .cta-banner::before {
      content: '';
      position: absolute;
      top: -50%; left: 50%;
      transform: translateX(-50%);
      width: 600px; height: 600px;
      background: radial-gradient(ellipse, rgba(196,135,58,0.2) 0%, transparent 65%);
      pointer-events: none;
    }
    .cta-banner h2 {
      font-family: 'Playfair Display', serif;
      font-size: clamp(28px, 4vw, 48px);
      color: var(--cream);
      margin-bottom: 1rem;
      letter-spacing: -0.02em;
      position: relative;
    }
    .cta-banner h2 em { color: var(--warm); font-style: italic; }
    .cta-banner p {
      font-size: 16px;
      color: rgba(245,240,232,0.55);
      margin-bottom: 2.5rem;
      font-weight: 300;
      position: relative;
    }
    .cta-banner-btns { display: flex; gap: 1rem; justify-content: center; position: relative; flex-wrap: wrap; }
    .btn-cream {
      padding: 14px 32px;
      background: var(--cream);
      color: var(--ink);
      border-radius: 100px;
      font-size: 15px;
      font-weight: 500;
      text-decoration: none;
      transition: opacity 0.2s;
      font-family: 'Outfit', sans-serif;
    }
    .btn-cream:hover { opacity: 0.85; }
    .btn-outline-cream {
      padding: 14px 32px;
      border: 1px solid rgba(245,240,232,0.25);
      color: var(--cream);
      border-radius: 100px;
      font-size: 15px;
      font-weight: 400;
      text-decoration: none;
      transition: background 0.2s;
      font-family: 'Outfit', sans-serif;
    }
    .btn-outline-cream:hover { background: rgba(245,240,232,0.1); }

    /* FOOTER */
    footer {
      border-top: 1px solid var(--border);
      padding: 2.5rem 3rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 1rem;
    }
    footer .nav-logo { font-size: 18px; }
    .footer-copy { font-size: 13px; color: var(--muted); }
    .footer-links { display: flex; gap: 1.5rem; }
    .footer-links a { font-size: 13px; color: var(--muted); text-decoration: none; }
    .footer-links a:hover { color: var(--ink); }

    /* ANIMATIONS */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes scroll {
      from { transform: translateX(0); }
      to   { transform: translateX(-50%); }
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
      nav { padding: 1rem 1.5rem; }
      .nav-links a:not(.nav-cta) { display: none; }
      .section { padding: 5rem 1.5rem; }
      .steps { grid-template-columns: 1fr; }
      .testi-grid { grid-template-columns: 1fr; }
      .cta-banner { margin: 0 1.5rem 5rem; padding: 3rem 1.75rem; }
      .hero-stats { gap: 2rem; }
      footer { padding: 2rem 1.5rem; }
      .cats-section { padding: 5rem 1.5rem; }
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="landing.html" class="nav-logo">ContractorHub</a>
  <div class="nav-links">
    <a href="#">How it works</a>
    <a href="#">Categories</a>
    <a href="#">For contractors</a>
    <a href="signin.html" class="nav-cta">Get started →</a>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-badge">Now live in Malaysia</div>
  <h1>Hire the <em>right</em> contractor,<br/>not just the closest one.</h1>
  <p class="hero-sub">ContractorHub connects you with verified professionals across construction, tech, design, and more — in minutes, not days.</p>
  <div class="hero-actions">
    <a href="signin.html" class="btn-main">Find a contractor</a>
    <a href="signin.html" class="btn-ghost">Join as a contractor</a>
  </div>
  <div class="hero-stats">
    <div class="hero-stat">
      <div class="hero-stat-num">1,240+</div>
      <div class="hero-stat-label">Verified Contractors</div>
    </div>
    <div class="hero-stat">
      <div class="hero-stat-num">98%</div>
      <div class="hero-stat-label">Satisfaction Rate</div>
    </div>
    <div class="hero-stat">
      <div class="hero-stat-num">4.8★</div>
      <div class="hero-stat-label">Average Rating</div>
    </div>
    <div class="hero-stat">
      <div class="hero-stat-num">24hr</div>
      <div class="hero-stat-label">Avg. Response Time</div>
    </div>
  </div>
</section>

<!-- MARQUEE -->
<div class="marquee-wrap">
  <div class="marquee-track">
    <div class="marquee-item"><span>●</span> Civil Engineering</div>
    <div class="marquee-item"><span>●</span> Interior Design</div>
    <div class="marquee-item"><span>●</span> Electrical Works</div>
    <div class="marquee-item"><span>●</span> Web Development</div>
    <div class="marquee-item"><span>●</span> Plumbing</div>
    <div class="marquee-item"><span>●</span> Renovation</div>
    <div class="marquee-item"><span>●</span> UI/UX Design</div>
    <div class="marquee-item"><span>●</span> Solar Installation</div>
    <div class="marquee-item"><span>●</span> HVAC</div>
    <div class="marquee-item"><span>●</span> Graphic Design</div>
    <div class="marquee-item"><span>●</span> Data Engineering</div>
    <div class="marquee-item"><span>●</span> Structural Engineering</div>
    <!-- duplicate for seamless loop -->
    <div class="marquee-item"><span>●</span> Civil Engineering</div>
    <div class="marquee-item"><span>●</span> Interior Design</div>
    <div class="marquee-item"><span>●</span> Electrical Works</div>
    <div class="marquee-item"><span>●</span> Web Development</div>
    <div class="marquee-item"><span>●</span> Plumbing</div>
    <div class="marquee-item"><span>●</span> Renovation</div>
    <div class="marquee-item"><span>●</span> UI/UX Design</div>
    <div class="marquee-item"><span>●</span> Solar Installation</div>
    <div class="marquee-item"><span>●</span> HVAC</div>
    <div class="marquee-item"><span>●</span> Graphic Design</div>
    <div class="marquee-item"><span>●</span> Data Engineering</div>
    <div class="marquee-item"><span>●</span> Structural Engineering</div>
  </div>
</div>

<!-- HOW IT WORKS -->
<section class="section">
  <p class="section-eyebrow">How it works</p>
  <h2 class="section-title">Three steps to your ideal contractor</h2>
  <div class="steps">
    <div class="step">
      <div class="step-num">01</div>
      <h3>Search & Filter</h3>
      <p>Browse by skill, category, location, or availability. Use smart filters to narrow down to exactly who you need.</p>
    </div>
    <div class="step">
      <div class="step-num">02</div>
      <h3>Review Profiles</h3>
      <p>Check verified ratings, past work, skills, and hourly rates. Every contractor is background-checked and verified.</p>
    </div>
    <div class="step">
      <div class="step-num">03</div>
      <h3>Request a Quote</h3>
      <p>Send a quote request directly. Get responses within 24 hours and hire with full confidence.</p>
    </div>
  </div>
</section>

<!-- CATEGORIES -->
<div class="cats-section">
  <div class="cats-inner">
    <p class="section-eyebrow">Browse by category</p>
    <h2 class="section-title">Whatever you need,<br/>we have the expert.</h2>
    <div class="cats-grid">
      <a href="signin.html" class="cat-card"><div class="cat-icon">🏗️</div><div class="cat-name">Construction</div><div class="cat-count">320+ pros</div></a>
      <a href="signin.html" class="cat-card"><div class="cat-icon">💻</div><div class="cat-name">Tech & Dev</div><div class="cat-count">210+ pros</div></a>
      <a href="signin.html" class="cat-card"><div class="cat-icon">🎨</div><div class="cat-name">Design</div><div class="cat-count">180+ pros</div></a>
      <a href="signin.html" class="cat-card"><div class="cat-icon">⚡</div><div class="cat-name">Electrical</div><div class="cat-count">145+ pros</div></a>
      <a href="signin.html" class="cat-card"><div class="cat-icon">🔧</div><div class="cat-name">Plumbing</div><div class="cat-count">98+ pros</div></a>
      <a href="signin.html" class="cat-card"><div class="cat-icon">🏠</div><div class="cat-name">Renovation</div><div class="cat-count">260+ pros</div></a>
      <a href="signin.html" class="cat-card"><div class="cat-icon">☀️</div><div class="cat-name">Solar</div><div class="cat-count">67+ pros</div></a>
      <a href="signin.html" class="cat-card"><div class="cat-icon">📐</div><div class="cat-name">Architecture</div><div class="cat-count">89+ pros</div></a>
    </div>
  </div>
</div>

<!-- TESTIMONIALS -->
<section class="section">
  <p class="section-eyebrow">What clients say</p>
  <h2 class="section-title">Trusted by hundreds<br/>across Malaysia</h2>
  <div class="testi-grid">
    <div class="testi-card">
      <div class="testi-stars">★★★★★</div>
      <p class="testi-text">"Found an excellent civil engineer within 2 hours. The verification process gave me complete peace of mind before hiring."</p>
      <div class="testi-author">
        <div class="testi-av">HM</div>
        <div><div class="testi-name">Hazrul M.</div><div class="testi-role">Property Developer, KL</div></div>
      </div>
    </div>
    <div class="testi-card">
      <div class="testi-stars">★★★★★</div>
      <p class="testi-text">"The quality of contractors here is exceptional. We hired a full-stack dev and launched our app in 6 weeks flat."</p>
      <div class="testi-author">
        <div class="testi-av">LK</div>
        <div><div class="testi-name">Liyana K.</div><div class="testi-role">Startup Founder, Penang</div></div>
      </div>
    </div>
    <div class="testi-card">
      <div class="testi-stars">★★★★☆</div>
      <p class="testi-text">"ContractorHub saved us weeks of sourcing. The filter by availability alone is worth it — no more chasing unresponsive vendors."</p>
      <div class="testi-author">
        <div class="testi-av">RS</div>
        <div><div class="testi-name">Raj S.</div><div class="testi-role">Operations Manager, JB</div></div>
      </div>
    </div>
  </div>
</section>

<!-- CTA BANNER -->
<div class="cta-banner">
  <h2>Ready to find your<br/><em>perfect match?</em></h2>
  <p>Join thousands of businesses already hiring smarter.</p>
  <div class="cta-banner-btns">
    <a href="signin.html" class="btn-cream">Get started — it's free</a>
    <a href="index.html" class="btn-outline-cream">Browse contractors →</a>
  </div>
</div>

<!-- FOOTER -->
<footer>
  <a href="landing.html" class="nav-logo">ContractorHub</a>
  <p class="footer-copy">© 2025 ContractorHub. All rights reserved.</p>
  <div class="footer-links">
    <a href="#">Privacy</a>
    <a href="#">Terms</a>
    <a href="#">Contact</a>
  </div>
</footer>

</body>
</html>
